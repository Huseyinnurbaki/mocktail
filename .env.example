# Mocktail Backend Configuration
#
# These environment variables configure the Mocktail Go API backend.
#
# HOW TO USE:
#
# 1. Local Development (Recommended):
#    Copy this file to .env and configure:
#      cp .env.example .env
#      # Edit .env with your values
#      make dev-api
#
# 2. Local Development (Shell):
#    Export in your shell before running make:
#      export MOCKTAIL_CORS_ORIGINS="http://localhost:3000,http://localhost:3001"
#      make dev-api
#
# 3. Docker:
#    Pass with -e flag:
#      docker run -e MOCKTAIL_CORS_ORIGINS="https://myapp.com" -p 4000:4000 hhaluk/mocktail
#
# 4. Docker Compose:
#    Add to docker-compose.yml under environment:
#      environment:
#        MOCKTAIL_CORS_ORIGINS: "https://myapp.com"
#        MOCKTAIL_CORS_CREDENTIALS: "true"

# ============================================
# CORS Configuration
# ============================================

# Allowed origins (comma-separated)
# Default: * (allow all)
# Examples:
#   MOCKTAIL_CORS_ORIGINS=https://myapp.com,http://localhost:3000
#   MOCKTAIL_CORS_ORIGINS=*
#
# MOCKTAIL_CORS_ORIGINS=*

# Allowed HTTP methods (comma-separated)
# Default: GET,POST,PUT,PATCH,DELETE,OPTIONS
#
# MOCKTAIL_CORS_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS

# Allowed headers (comma-separated)
# Default: * (allow all)
# Examples:
#   MOCKTAIL_CORS_HEADERS=Content-Type,Authorization,X-API-Key
#   MOCKTAIL_CORS_HEADERS=*
#
# MOCKTAIL_CORS_HEADERS=*

# Allow credentials (cookies, authorization headers)
# Default: false
# Set to "true" to enable
#
# ⚠️  IMPORTANT: Cannot use "*" for origins when credentials=true
#     You MUST specify exact origins (comma-separated)
#     Example: MOCKTAIL_CORS_ORIGINS=https://myapp.com,http://localhost:3000
#
# MOCKTAIL_CORS_CREDENTIALS=false

# ============================================
# API Key Authentication (Optional)
# ============================================

# Protect mock endpoints with API key
# If set, all requests to /mocktail/* must include this key
# Dashboard and Core API (/core/v1/*) are exempt
#
# Provide key via header or query param:
#   Header: X-API-Key: your-secret-key
#   Query:  ?api_key=your-secret-key
#
# If not set, mock endpoints are open (no authentication)
#
# MOCKTAIL_API_KEY=

# ============================================
# Frontend Configuration
# ============================================

# Override the Mocktail URL displayed in the dashboard
# Useful when deploying behind a reverse proxy or custom domain
#
# Examples:
#   MOCKTAIL_BASE_URL=https://api.mycompany.com/mocktail
#   MOCKTAIL_BASE_URL=https://mocktail.example.com/mocktail
#
# If not set, defaults to:
# - Development: http://localhost:4000/mocktail
# - Production: [your-domain]/mocktail
#
# MOCKTAIL_BASE_URL=
